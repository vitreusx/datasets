#!/usr/bin/bash
name="$1"

unzip -q -d "${name}" "archives/${name}.zip"
cd "${name}"

for subdir in $(find . -mindepth 1 -maxdepth 1 -type d); do
    cd "${subdir}"
    unzip -q -d images images.zip
    rm images.zip
    cd ..
done
