[project]
name = "datasets"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "dvc>=3.63.0",
    "dvc-s3>=3.2.2",
]

[tool.uv]
package = false
