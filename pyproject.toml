[project]
name = "datasets"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "dvc>=3.63.0",
    "dvc-s3>=3.2.2",
    "kaggle>=1.7.4.5",
    "numpy<2",
    "pillow>=12.0.0",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "ipykernel>=7.0.1",
    "jupyter>=1.1.1",
]
